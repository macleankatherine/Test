pipeline
{
   agent any
stages
   {
    stage('Build') {
      if (env.BUILD_NUMBER == '1')
      {
         currentBuild.result = 'ABORTED'
         sh 'echo "Dont build the first time"'
      }
    }
  }
} 
