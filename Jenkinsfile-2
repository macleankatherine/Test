pipeline {
    agent any
    stages {
        stage('Build') {
            when {
                expression {
                    "${BRANCH_NAME}" =~ /^(dev_|stable-).*/
                }
            }
            steps {
                sh 'echo "Hello World"'
            }
        }
        stage('Slay') {
            steps {
                    echo "Current branch name: ${BRANCH_NAME}"
            }
        }
    }
}
